import React from 'react'

export const metadata = {
  title: 'Login - Get Me a Chai',
  description: 'Manage your profile, view payments, and update your settings.',
}

export default function LoginLayout({ children }) {
  return <>{children}</>
}